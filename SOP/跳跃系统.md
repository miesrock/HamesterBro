# 跳跃系统 SOP

1. Player 在 `_ready()` 记录初始 Y 坐标 `_ground_y`
2. `_physics_process` 中检测 `_is_grounded()`
3. 接地时按 jump 设置 `velocity.y = jump_velocity`
4. 空中每帧 `velocity.y -= gravity * delta`
5. 掉到地面以下时复位到 `_ground_y`
6. `project.godot` 注册 jump 输入动作（Space）
